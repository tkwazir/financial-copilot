# Copy to .env.local. Server-only (not NEXT_PUBLIC_) — used by app/api/query/route.ts,
# never shipped to the client bundle. Defaults to http://127.0.0.1:8000 if unset.
BACKEND_URL=http://127.0.0.1:8000
